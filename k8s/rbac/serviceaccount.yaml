apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-dashboard-sa
  namespace: kubepulse
  labels:
    app.kubernetes.io/name: kubepulse
    app.kubernetes.io/component: backend
  annotations:
    description: "Read-only service account for KubePulse pod monitoring dashboard"
automountServiceAccountToken: true
